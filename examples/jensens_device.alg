begin
    real procedure SIGMA (i, h, k, t); value k; integer i, h, k; real t;
    begin real s;
        s := O;
        for i : = h step I until k do s : = t + s;
            SIGMA:= s
    end SIGMA
end
